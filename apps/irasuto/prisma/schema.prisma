datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URI")
}

generator client {
  provider = "prisma-client-js"
}

model Illustration {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  tweetId String @unique
}
