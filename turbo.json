{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", ".nuxt/**", ".svelte-kit/**", "dist/**", ".vercel/output/**"]
    },
    "dev": {
      "cache": false
    }
  }
}
